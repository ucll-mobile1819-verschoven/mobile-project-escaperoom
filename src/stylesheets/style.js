// @flow

import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    controls: {
        marginTop: 10
    },
    gameField: {
        flexDirection: 'row',
        flexWrap: 'wrap'
    },
});
